name: 'hevy_pipeline'
version: '1.0.0'
config-version: 2

profile: 'hevy_pipeline'

model-paths: ["models"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  hevy_pipeline:
    bronze:
      +schema: bronze
      +materialized: incremental
    gold:
      +schema: gold
      +materialized: table
